package sets;

interface Data extends Clonable, Comparable {

    /*
       This interface defines a type on which an ordering has been defined
       by compareTo() and from which copies can be made with clone().
    */
	
	

}